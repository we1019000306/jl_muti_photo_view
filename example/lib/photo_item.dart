class PhotoItem {
  PhotoItem(this.photo_width, this.photo_height, this.photo_dpi, this.rotationNum, this.scaleNum);
  double photo_width;
  double photo_height;
  double photo_dpi;
  double rotationNum;
  double scaleNum;
}